# v1.0 verification evidence matrix

This document is the evidence-driven completion gate for Berlin Urban Intelligence. It distinguishes implemented architecture from behavior that is actually verified.

## Status vocabulary

- **PASS** — the required behavior is implemented and backed by deterministic repository evidence and, where the gate is inherently operational, appropriate end-to-end/live evidence.
- **PARTIAL** — meaningful implementation/evidence exists, but an explicit acceptance requirement remains unverified or incomplete.
- **EXTERNAL/BLOCKED** — completion depends on an external provider or environment and the repository cannot legitimately fabricate success.
- **NOT VERIFIED** — the repository has no sufficient verification evidence for the gate.
- **NOT READY** — aggregate release/completion status while any critical gate is not PASS or the final candidate head has not yet passed its protected checks.

A unit test, architecture diagram or successful build is not by itself proof of end-to-end or scientific correctness. Point-in-time live smoke evidence proves provider compatibility only at that timestamp.

## Audit baseline

This matrix was established from current repository code, tests, documentation and GitHub Actions while addressing issue #11 on 2026-09-15 and is updated as independently scoped completion issues are verified.

Recent complete deterministic acceptance evidence includes:

- PR #8: viewport-backed map/detail inspection and route disruption acceptance, with backend/frontend/container/browser CI green before merge;
- PR #9: restored canonical/scenario invariants, with backend/frontend/container/browser CI green before merge;
- PR #10: repository governance documentation on final head `5b7f830691a8637f86d8fd8f2c0d7d6ec2ac7e3f`, GitHub Actions run `34962033938`, all required jobs `backend`, `frontend`, and `containers` successful;
- PR #22: repository-wide verification matrix and documentation-drift correction, merged after all protected checks passed;
- PR #23 / issue #12: RED integration tests first reproduced missing derived graph parity and missing relationship queries; subsequent code passed format/lint, strict mypy and the complete pytest suite before final documentation/CI verification;
- PR #26 / issue #13: real-agent descriptor completeness and composition-boundary work merged after all protected checks passed;
- PR #27 implementation head `f9c63349c821164f79a6d74c910730e955823c30`: GitHub Actions run `34980869733` passed `backend`, `frontend`, and `containers`, including Compose and Playwright, while real OSM provider verification remained deliberately separate;
- PR #28 implementation head `0e49f7644406af54964300b44468ce253c0fe4f2`: GitHub Actions run `34989105040` passed `backend`, `frontend`, and `containers`; the composed nginx → FastAPI → persisted state → React/MapLibre path passed all **6/6** Playwright cases, including populated derived lineage, source runtime status/error semantics, real agent health/capability/dependency metadata and snapshot reload diagnostics;
- PR #29 final implementation head `419e8a90c5c1c59d32a42cc8670cafa5bfcee0b4`: GitHub Actions run `34994941517` passed `backend`, `frontend`, and `containers`; browser acceptance passed **8/8** cases, including automated Axe checks for critical/serious WCAG A/AA findings, visible keyboard focus and the non-pointer resilience-routing path;
- PR #30 performance evidence run `35004770633`: the deterministic 10,000-stop/250-facility/750-node benchmark completed successfully and uploaded raw JSON latency/allocation evidence while recording `temporary_fixture_only=true` and `synthetic_production_fallback=false`;
- PR #31 / issue #18 final head `5ce9d5397694214869cb4204662a6ab72a279350`: protected CI run `35078122965` passed backend/frontend/container acceptance and security run `35078123100` passed Python runtime, frontend lockfile and browser-runner dependency audits before merge;
- PR #32 / issue #19 final head `3a7fc08fc0fc70ad78a1c09bb8909efff8acb529`: protected CI run `35079338962` passed backend, frontend and composed browser acceptance after RED observability contracts established source-refresh, snapshot-reload, derivation and scenario correlation/error semantics;
- PR #33 / issue #20 final head `e93cf83c25ce02e23f4672b4ab95c3a8011ddb63`: protected CI run `35083089572` and real-energy run `35083089585` passed before squash merge `42dfb85da6f80b46c2786c0ff108ec250bac8811`;
- PR #34 / issue #21: RED run `35083736052` first failed exactly on the missing second cross-domain contract; final head `be431495f1796b3bef23a52c8bf003fa32896b07` passed protected run `35090333735` plus real-energy run `35090333758` before squash merge `e562835a1cb60b7c9cbc7b38ad9c801fef66e814`;
- PR #35 / issue #14: protected run `35091630767` passed backend, frontend and containers on the road-smoke failover implementation head; independent live run `35091544852` acquired a real Berlin OSM road graph and completed the strict readiness/routing contract with `synthetic_fallback=false`; evidence-documentation head `9298108055f486e61c9979da302d451df24313e9` then passed protected run `35092998554` and real-energy run `35092998560`; and
- active repository ruleset `Protect main` requires the three protected GitHub Actions checks with strict/up-to-date branch semantics and no bypass actors.

The permanent correctness CI contract is `.github/workflows/ci.yml`. Live-source compatibility is intentionally separated into `.github/workflows/live-source-smoke.yml`; reference-source compatibility has a separate manual workflow, optional real OSM routing readiness has its own road-network smoke workflow, reproducible real Berlin energy evidence is isolated in `.github/workflows/energy-real-evaluation.yml`, and reproducible performance evidence is isolated in `.github/workflows/performance.yml`.

## Evidence matrix

| Gate | Status | Current evidence | Remaining evidence / issue |
|---|---|---|---|
| Modular source → canonical → agent/API architecture | **PASS** | `docs/architecture.md`; adapter/runtime/agent separation under `src/berlin_urban_intelligence/`; provider acquisition remains outside request handlers. | Reassess only if boundaries are changed. |
| Canonical contracts, epistemic state, UTC, units, CRS and finite numerics | **PASS** | `shared/contracts.py`; `tests/test_contracts.py`; PR #9 restored rejection of `NaN`/±infinity. | None for current contract version. |
| Provenance retained across canonical values | **PASS** | `Provenance` contract; adapter/data-pipeline tests; RDF projection; map canonical detail inspector; `docs/data/provenance.md`; semantic relationship queries expose PROV links; PR #28 browser acceptance renders persisted derivation provenance/upstream lineage. | Re-test when new provenance-bearing resource types are added. |
| No fabricated production measurements/fallbacks | **PASS** | Explicit unavailable/unknown semantics; `scripts/check_production_data.py`; deterministic fixtures are CI/benchmark-only; runtime/source tests and docs. Road-network smoke and performance evidence explicitly record `synthetic_fallback=false`. | Continue enforcing on future integrations. |
| Live DWD/Berlin LQI/VBB acquisition, freshness and failure semantics | **PASS** | `runtime/refresh.py`; source-status models/tests; scheduled/manual `live-source-smoke.yml`; point-in-time successful live smoke documented for 2026-09-14. | Provider availability can regress externally; scheduled smoke remains operational evidence, not a permanent guarantee. |
| Official facility/climate/VBB-static reference acquisition | **PARTIAL** | `ReferenceRefreshCoordinator`, `ReferenceAcquisitionCoordinator`; deterministic reference tests; `reference-smoke-once.yml` verifies non-empty facilities/climate/stops when run. | Workflow is not permanent scheduled monitoring; this is an operational-monitoring limitation rather than a missing v1 correctness path. |
| Atomic persisted state and last-known-good replacement semantics | **PASS** | `runtime/atomic.py`, state stores, `runtime/reload.py`; `tests/test_atomic_storage.py`, `tests/test_api.py`; invalid replacement keeps previous validated in-process state. OSM provider/schema tests preserve the previous valid road network and never fabricate a replacement. | Multi-process synchronized rollout is outside current single-process deployment guarantee. |
| Automatic runtime/reference/energy/derived hot reload | **PASS** | `_ApiStateController` watches all four persisted states before requests; frontend snapshot-token polling; API regression tests; PR #28 browser acceptance verifies current/current/current/missing reload diagnostics against persisted acceptance state. | Compatibility documentation drift found by the audit has been corrected. |
| Source-specific cadence / persistent live refresh worker | **PASS** | `runtime/worker.py`; `scripts/refresh_live.py --interval-seconds`; Compose `refresh` service defaults to configurable 300 s interval; reference/energy operations remain separate. | No claim that every slow source is auto-scheduled. |
| Six domain/aggregation agents exist and expose health | **PASS** | Live State, Mobility, Exposure, Heat, Energy, Resilience agents; `/api/v1/agents` and `/agents/health`; deterministic domain/API tests; PR #28 verifies populated agent health in the composed browser path. | Re-test when the real agent set changes. |
| Real-agent descriptor completeness and cross-agent boundary discipline | **PASS** | All six real descriptors expose explicit name/domain/capabilities/input/output metadata; domain agents declare source dependencies where applicable; Live State declares required dependencies on the five domain agents; `tests/test_real_agent_contracts.py` verifies real DAG/missing dependency behavior; `LiveStateAgent` consumes typed `AgentHealth` mappings only; `/api/v1/agents/health` performs domain-health composition outside the agent; `tests/test_agent_api_contract.py` preserves public API semantics; PR #28 browser acceptance verifies Live State capability/dependency/health rendering. | Re-test when adding agents or moving composition boundaries. |
| Registry-driven deterministic workflow resolution | **PASS** | `AgentRegistry.resolve_capability()` owns unique capability resolution and ambiguity errors; dependency order remains registry-driven; `Orchestrator` consumes the registry resolver; `tests/test_registry_capability_resolution.py`, `tests/test_orchestrator.py`, `tests/test_agent_registry.py`. | Re-test as new agent dependencies/capabilities are introduced. |
| Derivation definitions, provenance, freshness/status and dependency DAG | **PASS** | `knowledge/derivations.py`, `runtime/derived.py`, `shared/dependencies.py`; derivation/dependency tests cover cycles, stale propagation and execution failures; PR #34 extends the same persisted contract to two explicitly documented cross-domain contexts. | Re-test when derivation contracts or dependency semantics change. |
| Dependency-aware incremental derived recomputation | **PASS** | `runtime/derived_refresh.py`, `knowledge/execution.py`; `tests/test_derived_refresh.py`, `tests/test_derivation_execution.py`; unaffected records are retained. | None for current persisted derived products. |
| Source-backed derived products avoid synthetic fallback/composite score | **PASS** | `DerivedProductBuilder` emits VBB delayed-update share, Heat + Air Quality context and Mobility + Air Quality context only when required inputs exist. `tests/test_derived_products.py` covers missing data, source unavailability, quality/freshness propagation and explicit no-score semantics. | Re-test when new cross-domain products are introduced. |
| Semantic RDF projection of canonical state | **PASS** | `knowledge/graph.py`, ontology/shapes, `tests/test_knowledge_graph.py`, `scripts/validate_knowledge.py`; API graph builds from validated runtime/reference/energy/derived state. | Re-test when semantic resource types change. |
| Derived lineage in semantic projection | **PASS** | `knowledge/derived_graph.py` projects derivation definitions/records and PROV lineage; `refresh_live.py` includes it in generated RDF; `/api/v1/graph` projects persisted `DerivedState`; `tests/test_semantic_api.py` verifies definition and upstream lineage triples. | None for current derived graph contract. |
| Semantic relationships used as an inspectable/queryable API capability | **PASS** | `knowledge/relations.py`; `/api/v1/knowledge/relations/{resource_id}` returns bounded deterministic incoming/outgoing RDF relations with node kind and truncation metadata; integration tests cover observation, facility, forecast, derived, missing resource and bounding. | Complete graph serialization is documented as a bulk/research path rather than a high-QPS endpoint. |
| Real Berlin road-network/reference completeness | **PASS** | OSM acquisition adapter/coordinator; strict `runtime/road_readiness.py`; deterministic readiness/imputation/failure/LKG tests; multi-endpoint `.github/workflows/road-network-smoke.yml`; live run `35091544852` acquired `Mitte, Berlin, Germany` through `https://overpass-api.de/api` after retaining two timed-out endpoint attempts. The accepted snapshot contained **740 nodes** and **1,800 edges**, provider `OpenStreetMap contributors`, ODbL 1.0 provenance, provenance-visible speed/travel-time imputation and `synthetic_fallback=false`. The existing `ResilienceAgent` completed route `osm-node:10087214573` → `osm-node:13043292535` at approximately 2.139 s. Evidence artifact `10445300085`; see `docs/road-network-verification.md`. | Point-in-time provider success is not a permanent availability guarantee; scheduled/manual smoke must continue to expose future provider failures rather than fabricate topology. |
| Resilience routing/accessibility and immutable disruption overlays | **PASS** | `agents/resilience.py`; parallel-edge, snapping and scenario tests; Playwright baseline 100 s → disrupted 160 s acceptance fixture; real live run `35091544852` also proves the readiness verifier can route over an acquired OSM snapshot; PR #29 adds an equivalent coordinate-based keyboard route-selection path over the same nearest-node and resilience APIs. | Re-test when routing contracts or network-normalization semantics change. |
| Scenario validation and baseline isolation | **PASS** | `scenario_engine/`; scenario tests; PR #9 requires explicit heat/energy deltas and rejects stale/future/invalid baselines; route overlays copy baseline graph. | None for current supported scenario kinds. |
| Baseline/scenario/difference presented distinctly in dashboard | **PASS** | Map-selected route comparison renders baseline/scenario and delta; Playwright verifies 100 s baseline, 160 s scenario, +60 s/+60%. | None for the current route-comparison surface. |
| Berlin Energy evaluation/forecast implementation | **PASS** | Leakage-safe chronological pipeline, baseline/candidate metrics, dataset fingerprint binding and `EnergyAgent` acceptance checks have deterministic tests. | Re-test when the forecasting contract or accepted source semantics change. |
| Current reproducible real Berlin energy artefact | **PASS** | `.github/workflows/energy-real-evaluation.yml` downloads official Stromnetz Berlin sources without committing them. All five inspected 2025 curves expose a 96-row upstream `#BEZUG!` timestamp defect and are deliberately not repaired. The clean official 2024 high-voltage curve has 35,136 validated quarter-hour points; run `35092998560` reconfirmed strict source validation, chronological evaluation, persistence, matching fingerprint checks and `/api/v1/energy` exposure on the PR #35 evidence head. `docs/energy-real-evidence.md` records source scope, measured metrics and limitations. | Historical high-voltage evidence is intentionally not claimed as current total Berlin demand or a live grid-control forecast. Re-run against a later clean official publication when available. |
| Multiple defensible source-backed cross-domain workflows | **PASS** | `heat-air-quality-context-v1` and `mobility-air-quality-context-v1` are source-backed descriptive products with explicit independent timestamps, inputs, producer/algorithm versions, provenance, quality/freshness and missing/unavailable behavior. PR #34 began with RED contract run `35083736052`; deterministic tests cover normal/missing/source-unavailable cases, and composed Playwright acceptance switches between both populated persisted results and displays their scientific interpretation. `docs/evaluation/cross-domain-workflows.md` defines the allowed interpretations and non-claims. | No causal, exposure, correlation or universal-score inference is claimed; future cross-domain products require the same evidence contract. |
| Partial failure isolation across cross-domain assessment | **PASS** | `IntegratedAssessmentService` returns per-dimension unavailable/error state rather than fabricating or failing the whole response; derived cross-domain records also disappear on missing required inputs while last-known-good data carry unavailable/stale source freshness. | Re-test when new workflows are added. |
| API exposes system/source/agent/domain/reference/scenario/derived/dependency/map/semantic surfaces | **PASS** | FastAPI routes and OpenAPI generation gate; API/map/derived/semantic tests; bounded list/map/relation endpoints; `tests/test_source_registry_api.py` verifies the deployment-safe `BUI_SOURCE_REGISTRY` override used by Compose. | Reassess when API contracts expand. |
| Map-optimized bounded reference API | **PASS** | `/api/v1/map/reference` bbox filtering, per-layer total/matched/returned/truncated metadata, canonical detail endpoint and API/unit/browser tests; PR #30 additionally bounds retained temporary selections and measures the 10,000-stop two-layer viewport at p95 193.779 ms on its recorded hosted runner while returning at most 250 features per requested layer. | CPU matching remains O(n) per requested layer; a spatial index/database is the documented next step if actual deployment scale requires it. |
| Dashboard analytical map and layer interaction | **PASS** | MapLibre viewport loads, layer toggles, canonical object inspection, routing selection, scenario overlay and external-style fallback are implemented/tested; critical routing no longer depends exclusively on pointer map selection because PR #29 provides a keyboard coordinate alternative. | Arbitrary map feature exploration remains primarily visual; its limitation is documented in `docs/accessibility.md`. |
| Dashboard provenance/derived/platform inspector implementation | **PASS** | `DerivedInspector`, `PlatformInspector`, canonical reference inspector and source/agent endpoints exist; PR #34 adds the derivation definition description as an explicit scientific-interpretation field and browser acceptance verifies both cross-domain products, inputs and caveats through persisted state. | Re-test when inspector contracts change. |
| End-to-end browser acceptance of principal workflow chain | **PASS** | Existing PR #29 acceptance covers the principal map/scenario/accessibility chain; PR #34 extends the composed nginx → FastAPI → persisted-state → React path to two populated cross-domain products and verifies their lineage and scientific interpretation. | Expand when new principal workflows become part of v1 scope. |
| Docker/Compose single-host deployment topology | **PASS** | Backend, frontend and persistent refresh services; CI validates Compose, builds both images, starts stack, checks backend/frontend/proxy and runs Playwright; backend explicitly receives persisted-state and source-registry paths. | This is not a horizontally scaled/multi-tenant deployment guarantee. |
| CI quality gates and protected merge workflow | **PASS** | Ruff format/lint, strict mypy, pytest+coverage, knowledge validation, production-data guard, secret guard, OpenAPI, frontend test/build, Docker/Compose/Playwright; protected run `35092998554` passed all three required jobs on the evidence-documentation head. Active `Protect main` requires `backend`, `frontend`, `containers` with strict/up-to-date semantics. | Keep ruleset/check names synchronized with workflow changes. |
| Structured API request observability | **PASS** | JSON formatter, operation ID, request error state and duration, `X-Operation-Id`; allow-listed logging fields avoid arbitrary payload context. | Re-test when request logging fields or trust boundaries change. |
| Refresh/derivation/scenario structured observability | **PASS** | PR #32 adds `observe_operation` plus correlated `source_refresh`, `snapshot_reload`, `derivation_refresh` and `scenario_calculation` events. Tests cover success/failure duration/error state, raw-error redaction, no-change reload suppression, shared refresh correlation, derivation refresh and scenario operation-ID propagation; protected run `35079338962` passed backend/frontend/container acceptance. | Keep event fields low-cardinality and allow-listed as new operation boundaries are added. |
| Security baseline | **PASS** | PR #31 establishes loopback-only default exposure, unprivileged/no-new-privileges containers, root-owned code/config with bounded writable paths, browser security headers, exception-message redaction, `SECURITY.md`/`docs/security.md` and separate Python/frontend/browser dependency auditing. Final protected run `35078122965` and security audit run `35078123100` passed. | Public multi-tenant/authenticated deployment remains outside the documented single-host research trust boundary and requires a gateway/auth/TLS design rather than fake in-app credentials. |
| Performance/load/scaling evidence | **PASS** | `src/berlin_urban_intelligence/performance.py`, `scripts/benchmark_performance.py`, `.github/workflows/performance.yml`, `tests/test_performance_baseline.py` and `docs/performance.md`; run `35004770633` records reproducible p50/p95/max latency and peak traced allocations for snapshot checks, system/agent/map/detail/nearest/routing/graph paths with explicit fixture and runner metadata. Stable correctness CI asserts no-change reload and bounded-map invariants instead of brittle hosted-runner timing thresholds. | No production capacity or concurrency/soak guarantee is claimed; heavier deployment-specific load testing remains appropriate only if a production topology is defined. |
| Accessibility evidence | **PASS** | `frontend/e2e/accessibility.spec.ts`; pinned `@axe-core/playwright`; PR #29 run `34994941517` passed Axe scans with no critical/serious configured WCAG A/AA violations in the tested principal states, visible-focus assertions and keyboard-only workflow/layer/scenario/inspector/routing interaction. `docs/accessibility.md` records the non-pointer route, manual keyboard/zoom/reflow/screen-reader checklist and explicit map/claim limitations. | This is a practical regression baseline, **not** a formal WCAG conformance claim or external audit. Re-run manual checks for substantial UI/release changes. |
| Documentation/reproducibility consistency | **PASS** | Primary docs, deterministic commands, source registry, architecture/evaluation/research sections and verification matrix are present; #11 corrected stale restart/orchestrator claims, #12 documents semantic API parity, #13 documents agent composition, #14 now records successful real OSM network/routing evidence while retaining prior provider failures, #15 verifies populated inspectors, #16 establishes the accessibility baseline, #17 records reproducible performance/scaling evidence, #18 documents the security trust boundary, #19 documents structured operation observability, #20 records reproducible real Berlin energy evidence and #21 documents cross-domain interpretation boundaries. | Maintain documentation in the same feature PR when contracts/behavior change. |
| Release/version readiness | **PASS** | All identified critical v1.0 correctness/evidence gates are satisfied. Real OSM run `35091544852`, protected run `35092998554`, real-energy run `35092998560` and the completed independent PR review support the candidate. | Creating a `v1.0.0` release/tag is a separate product/versioning action and still requires deliberate release notes; PASS here means the evidence gate is satisfied, not that a release has already been published. |

## Critical open work before v1.0 can be considered

There is no unresolved critical domain/evidence gap in the current v1.0 matrix. Issue #14's previously blocked real Berlin OSM requirement is satisfied by live run `35091544852` and artifact `10445300085`, and the evidence-documentation candidate passed all protected required checks in run `35092998554`.

The aggregate v1.0 evidence gate is therefore **PASS**. Public-provider availability, historical-energy scope, accessibility limitations, single-host deployment assumptions and performance caveats remain documented limitations rather than hidden completion gaps.

## Completion rule

A v1.0 readiness review may set the aggregate gate to **PASS** only when:

1. every critical row is PASS or explicitly accepted as a documented non-goal for the intended v1 deployment/research scope;
2. the candidate head passes all protected required checks (`backend`, `frontend`, `containers`);
3. relevant live/reference/energy smoke evidence is current enough for the release claim being made;
4. no known critical defect, fabricated production fallback, unresolved review finding or misleading documentation remains; and
5. release notes state scientific/operational limitations instead of implying municipal-authority or causal guarantees.

The current candidate satisfies the evidence gate above. Publishing an actual semantic version remains a separate release-management decision.