# ADR 005 — No universal master score

**Status:** Accepted

Cross-domain results are reported as separate dimensions rather than collapsed into one Berlin score.

A composite would require normative weights, normalization choices and uncertainty propagation that are not justified by the current evidence. Keeping dimensions explicit is more transparent and prevents precision theatre.
